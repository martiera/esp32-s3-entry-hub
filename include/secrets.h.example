#ifndef SECRETS_H
#define SECRETS_H

// WiFi Credentials (fallback if WiFiManager not configured)
#define WIFI_SSID "your-wifi-ssid"
#define WIFI_PASSWORD "your-wifi-password"

// OTA Password
#define OTA_PASSWORD "your-ota-password"

// API Keys
#define WEATHER_API_KEY "your-openweather-api-key"
#define PORCUPINE_ACCESS_KEY "your-porcupine-access-key"

// Home Assistant (optional - can be configured via web UI)
#define HA_BASE_URL "http://homeassistant.local:8123"
#define HA_TOKEN "your-long-lived-access-token"

// Note: MQTT settings are now configured via the web UI (Integrations page)

#endif
